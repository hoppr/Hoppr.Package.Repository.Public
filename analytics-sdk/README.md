# analytics-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build analytics-sdk` to build the library.

## Running unit tests

Run `nx test analytics-sdk` to execute the unit tests via [Jest](https://jestjs.io).
